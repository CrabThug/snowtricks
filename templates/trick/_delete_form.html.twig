<form class="w3-modal-content w3-card-4" method="post" action="{{ path('trick_delete', {'slug': trick.slug}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick.id) }}">
    <header class="w3-container w3-blue w3-center">
        <h2>Confirmation</h2>
    </header>
    <div class="w3-container w3-center w3-margin w3-padding-32">
        <p class="w3-margin">Voulez vous supprimer le trick ?</p>
    </div>
    <footer class="w3-container w3-center w3-margin w3-padding-32">
        <button class="w3-button w3-red">Confirmer</button>
        <a class="w3-button" onclick="document.getElementById('{{'modal' ~ trick.id }}').style.display='none'">Annuler</a>
    </footer>
</form>
