{% extends 'base.html.twig' %}

{% block title %}- réinitialisation{% endblock %}

{% block body %}
    <div class="w3-light-gray w3-padding-64">
        <div class="w3-center w3-white w3-padding-64 w3-round" style="max-width: 600px; margin: auto">
            <h3 class="w3-center">Réinitialisation de mot de passe</h3>
            {{ form_start(askResetPasswordForm) }}
            {{ form_widget(askResetPasswordForm) }}
            {{ form_end(askResetPasswordForm) }}
        </div>
    </div>
{% endblock %}

{% block footer %}{% endblock footer %}
